import Screen from '../../components/Screen';

// Mobile shell — mirrors aied-web/src/screens/MockTest. Wire to services/api.js (same endpoints as web).
export default function MockTest() {
  return <Screen title="Mock test" lead="Adaptive questions, graded instantly." />;
}
