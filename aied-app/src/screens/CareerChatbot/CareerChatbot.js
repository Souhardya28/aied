import Screen from '../../components/Screen';

// Mobile shell — mirrors aied-web/src/screens/CareerChatbot. Wire to services/api.js (same endpoints as web).
export default function CareerChatbot() {
  return <Screen title="Career guide" lead="Streams, exams and colleges that fit your marks." />;
}
