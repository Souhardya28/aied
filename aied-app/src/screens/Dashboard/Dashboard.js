import Screen from '../../components/Screen';

// Mobile shell — mirrors aied-web/src/screens/Dashboard. Wire to services/api.js (same endpoints as web).
export default function Dashboard() {
  return <Screen title="Progress" lead="Syllabus done, time spent, weak topics." />;
}
