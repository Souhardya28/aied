import Screen from '../../components/Screen';

// Mobile shell — mirrors aied-web/src/screens/PYQAnalysis. Wire to services/api.js (same endpoints as web).
export default function PYQAnalysis() {
  return <Screen title="Past papers" lead="Which chapters carry the marks." />;
}
