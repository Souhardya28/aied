import Screen from '../../components/Screen';

// Mobile shell — mirrors aied-web/src/screens/VideoPlayer. Wire to services/api.js (same endpoints as web).
export default function VideoPlayer() {
  return <Screen title="Watch" lead="Transcript, translation and narration beside the video." />;
}
