# aied-app (React Native / Expo)

Mobile shell with the same screen structure as the web app. Each screen is a stub wired to the
same Express API through `src/services/api.js`. The hackathon demo runs on `aied-web`; port screens here after.

    npm install && npx expo start
